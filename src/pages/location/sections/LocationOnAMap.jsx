import React from "react";
import RedirectingBtn from "../../../components/RedirectingBtn";

const LocationOnAMap = () => {
  return (
    <section>
      <RedirectingBtn switch oppositeArrow text="BACK TO HOME" to="/" />
    </section>
  );
};

export default LocationOnAMap;
